name: support-agent-env
version: 1.0.0
description: Customer support environment

tasks:
  - id: easy
    difficulty: easy
    max_steps: 1
  - id: medium
    difficulty: medium
    max_steps: 1
  - id: hard
    difficulty: hard
    max_steps: 1

rewards:
  range: [0.01, 0.99]
  partial_credit: true
  clip_fn: clamp_score
